# ---- Core App ----
streamlit
python-dotenv
requests
google-generativeai

# ---- Text & Speech (Cloud-compatible) ----
gTTS  # Works online; use st.audio() to play the output
# SpeechRecognition  # Needs microphone, not supported on Streamlit Cloud
# pyttsx3  # Requires system-level audio (not supported)
# pyaudio  # Causes build failure (needs portaudio)
# plyer  # Not needed for web
# simpleaudio  # Not supported on Streamlit Cloud

# ---- Data & Parsing ----
dateparser
beautifulsoup4
duckduckgo-search

# ---- Local Agents ----
pandas
jsonschema

# ---- Optional AI / Email helpers ----
email-validator

# ---- For Google Calendar Sync (optional but recommended) ----
google-api-python-client
google-auth
google-auth-oauthlib
google-auth-httplib2

# ---- System / Utility ----
urllib3
certifi
idna
charset-normalizer
